# Chapter 1: Numbers and Arithmetic

This chapter introduces the language's numeric core. The tests establish that Trivial supports integer and floating-point literals, negative numbers, and the four basic arithmetic operations.

The suite also introduces operator precedence and parentheses, which is where every small language stops being cute and starts needing an actual parser. By the end of this chapter, students should understand that expressions evaluate to values and that grouping changes meaning.
